/*
public class BubbleSort {
	private int vetor[];

	public void funcBubble(int[] x){
		int aux;
		boolean controle;
		setVetor(x);
		
		for (int i = 0; i < vetor.length; i++) {
			controle = true;
			for (int j = 0; j < (vetor.length - 1); j++) {
				if (vetor[j] > vetor[j + 1]) {
					aux = vetor[j];
					vetor[j] = vetor[j + 1];
					vetor[j + 1] = aux;s
					controle = false;
				}
			}
			if (controle) {
				break;
			}
		}
		for (int i = 0; i < vetor.length; i++) {
			System.out.println(vetor[i] + "");
		}
		
		
	}

	public int[] getVetor() {
		return vetor;
	}

	public void setVetor(int vetor[]) {
		this.vetor = vetor;
	}
	


}
*/